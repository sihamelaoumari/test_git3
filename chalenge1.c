#include <stdio.h>

int main() {
    for (int i = 1; i <= 100; i++) {
        printf("%d\n", i);
    }
    return 0;
}

// login github

// telecharger git  https://git-scm.com/downloads/win

// setup git installation

// config username and email for git  git config --global user.name "Mona Lisa", git config --global user.email "YOUR_EMAIL"


// method 1
// create repo "test_git " in github

// create dossier in pc test_git "

// inisialize local git repo in dossier  'git init'
// create new file 

// add changes to staging area  " git add ."

// create commit " git commit -m "message"

// connect local repo with github repo "git remote add origin url-to-put"

// push changes to github repo " git push -u origin main"


// method 2